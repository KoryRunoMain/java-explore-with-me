package ru.practicum.privateApi.participation;

public class PrivateParticipationController {

    // GET: path = "/users" + "/{userId}/requests"

    // POST: path = "/users" + "/{userId}/requests"

    // PATCH: path = "/users" + "/{userId}/requests/{requestId}/cancel"

}
