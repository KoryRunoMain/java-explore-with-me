package ru.practicum.privateApi.event;

import lombok.RequiredArgsConstructor;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.bind.annotation.RestController;

@RestController
@RequiredArgsConstructor
@RequestMapping()
public class PrivateEventController {

    // GET: path = "/users" + "/{userId}/events"

    // POST: path = "/users" + "/{userId}/events"

    // GET: path = "/users" + "/{userId}/events/{eventId}"

    // PATCH: path = "/users" + "/{userId}/events/{eventId}"

    // GET: path = "/users" + "/{userId}/events/{eventId}/requests"

    // PATCH: path = "/users" + "/{userId}/events/{eventId}/requests"

}
